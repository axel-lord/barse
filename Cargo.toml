[package]
name = "parse-common"
version = "0.1.0"
edition = "2021"

[features]
derive = ["dep:parse-derive"]

[dependencies]
parse-derive = { path = "../parse-derive", optional = true }
thiserror = "1.0.40"
